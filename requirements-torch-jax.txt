# Dependencias opcionales de PyTorch/JAX.
#
# Solo se usan en unidades/clasificadores/capitulo_pytorch_jax.qmd (y de forma
# puntual en unidades/clustering/capitulo_tsne_umap.qmd). No se instalan en el
# venv principal (.env) para ahorrar espacio (~800MB): renderiza esos
# capítulos desde un venv aparte con la build de torch/jax adecuada para tu
# equipo (CUDA/MPS/CPU), instalando este archivo ahí.
#
#   python -m venv .venv-torch-jax
#   .venv-torch-jax/bin/pip install -r requirements.txt -r requirements-torch-jax.txt

chex==0.1.90
etils==1.5.2
filelock==3.19.1
flax==0.8.5
fsspec==2025.10.0
jax==0.4.30
jaxlib==0.4.30
ml_dtypes==0.5.4
msgpack==1.1.2
networkx==3.2.1
opt_einsum==3.4.0
optax==0.2.4
orbax-checkpoint==0.6.4
rich==15.0.0
sympy==1.14.0
tensorstore==0.1.69
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
