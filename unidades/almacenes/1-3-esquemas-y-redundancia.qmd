---
title: "Esquemas Dimensionales y Redundancia"
---

En el capítulo anterior se definieron hechos, dimensiones, granularidad y aditividad. Ahora corresponde decidir *cómo* se estructuran físicamente las dimensiones alrededor del hecho: qué tan normalizadas están, cuánta redundancia se acepta y qué impacto real tiene esa decisión sobre el costo y la velocidad de consulta.

## Esquema de Estrella

El esquema estrella consiste en una tabla de hechos central conectada
directamente a varias dimensiones. Sus dimensiones suelen estar
desnormalizadas (redundancia controlada) para reducir joins y optimizar
consultas analíticas.

En el ejemplo académico, la dimensión estudiante incluye atributos como
carrera y facultad en la misma tabla, lo que simplifica consultas
típicas del estilo "promedio por carrera y por semestre". El costo es
aceptar repetición (por ejemplo, repetir "ciencias" como facultad en
muchos registros).

Desde el punto de vista estructural, el modelo puede representarse así:

```{mermaid}
erDiagram

    HECHO_EVALUACION_ACADEMICA {
        int id_estudiante
        int id_asignatura
        int id_tiempo
        decimal calificacion
        int creditos
        int indicador_reprobado
    }

    DIMENSION_ESTUDIANTE {
        int id_estudiante
        varchar nombre
        varchar sexo
        varchar generacion
        varchar carrera
        varchar facultad
    }

    DIMENSION_ASIGNATURA {
        int id_asignatura
        varchar nombre_asignatura
        varchar tipo_asignatura
    }

    DIMENSION_TIEMPO {
        int id_tiempo
        varchar semestre
        int anio
        varchar periodo
    }

    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_ESTUDIANTE : pertenece_a
    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_ASIGNATURA : corresponde_a
    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_TIEMPO : ocurre_en
```

En este esquema:

-   La tabla de hechos está al centro.
-   Todas las dimensiones se conectan directamente a ella.
-   No existen relaciones entre dimensiones.
-   La información jerárquica (por ejemplo, carrera → facultad) se
    almacena dentro de la misma dimensión.

## Esquema de Copo de Nieve (Snowflake)

El esquema de copo de nieve normaliza las dimensiones en subdimensiones.
Por ejemplo, separar "carrera" y "facultad" en tablas propias
relacionadas jerárquicamente, evitando redundancia, pero aumentando
joins y complejidad de consulta.

```{mermaid}
erDiagram

    HECHO_EVALUACION_ACADEMICA {
        int id_estudiante
        int id_asignatura
        int id_tiempo
        decimal calificacion
        int creditos
        int indicador_reprobado
    }

    DIMENSION_ESTUDIANTE {
        int id_estudiante
        varchar nombre
        varchar sexo
        varchar generacion
        int id_carrera
    }

    DIMENSION_CARRERA {
        int id_carrera
        varchar nombre_carrera
        int id_facultad
    }

    DIMENSION_FACULTAD {
        int id_facultad
        varchar nombre_facultad
    }

    DIMENSION_ASIGNATURA {
        int id_asignatura
        varchar nombre_asignatura
        varchar tipo_asignatura
    }

    DIMENSION_TIEMPO {
        int id_tiempo
        varchar semestre
        int anio
        varchar periodo
    }

    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_ESTUDIANTE : pertenece_a
    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_ASIGNATURA : corresponde_a
    HECHO_EVALUACION_ACADEMICA }o--|| DIMENSION_TIEMPO : ocurre_en

    DIMENSION_ESTUDIANTE }o--|| DIMENSION_CARRERA : estudia
    DIMENSION_CARRERA }o--|| DIMENSION_FACULTAD : pertenece
```

En este esquema:

-   La dimensión estudiante ya no almacena directamente la facultad.
-   Carrera y facultad se modelan como entidades independientes.
-   Se elimina redundancia.
-   Se incrementa el número de joins necesarios para consultas que
    involucren facultad.

### Comparación conceptual

  Aspecto                  Estrella                Copo de nieve
  ------------------------ ----------------------- ------------------
  Normalización            Baja (desnormalizada)   Alta
  Redundancia              Controlada              Reducida
  Número de joins          Menor                   Mayor
  Complejidad conceptual   Más simple              Más estructurada
  Rendimiento OLAP         Generalmente mejor      Puede disminuir
  Gobierno de datos        Más flexible            Más controlado

### Lo mismo, en SQL

Con un dominio distinto (ventas), la diferencia entre ambos esquemas se ve directamente en la consulta:

```sql
-- Estrella: un solo join por dimensión
select
  d.anio,
  p.categoria,
  sum(f.importe) as ventas
from fact_ventas as f
join dim_fecha as d using (fecha_key)
join dim_producto as p using (producto_key)
group by d.anio, p.categoria;
```

```sql
-- Copo de nieve: dim_producto no tiene "categoria", hay que subir un nivel más
select
  d.anio,
  c.categoria,
  sum(f.importe) as ventas
from fact_ventas as f
join dim_fecha as d using (fecha_key)
join dim_producto as p using (producto_key)
join dim_categoria as c using (categoria_key)
group by d.anio, c.categoria;
```

| Criterio | Estrella | Copo de nieve |
|---|---|---|
| Consultas | Simples, pocas uniones | Más uniones |
| Redundancia | Mayor | Menor |
| Mantenimiento | Directo | Más entidades y claves |
| Uso recomendado | BI y autoservicio | Jerarquías grandes o compartidas |

## Redundancia controlada: qué cuesta realmente desnormalizar

La comparación anterior suele quedarse en "el copo de nieve reduce redundancia", como si repetir datos fuera automáticamente malo. En un almacén de datos columnar (Parquet, Delta, BigQuery, Snowflake) esa redundancia casi no cuesta lo que parece.

Un motor columnar no almacena cada valor de texto tal cual: aplica **codificación por diccionario** (cada valor distinto se guarda una sola vez y cada fila solo referencia un código corto) y **run-length encoding** (RLE) sobre columnas ordenadas o de baja cardinalidad. Si `dim_producto` tiene 500,000 filas y la columna `categoria` solo tiene 20 valores distintos, el motor no almacena "Electrónica" 25,000 veces: almacena el diccionario de 20 categorías una vez y, por fila, un índice de unos cuantos bits.

En la práctica esto significa que:

- Guardar `categoria` directamente en `dim_producto` (estrella) cuesta, en almacenamiento, un orden de magnitud similar a guardar `categoria_key` (copo de nieve) — ambos terminan siendo, a nivel de disco, un entero pequeño por fila.
- El ahorro de espacio que promete la normalización es, en un motor columnar moderno, marginal frente a lo que cuesta en 1990 con motores orientados a fila.
- Lo que sí cambia entre ambos esquemas no es tanto el almacenamiento, sino el **costo de consulta**.

## Costo de consulta: joins y motores en la nube

El argumento a favor de la estrella no es "ocupa menos espacio", es "cada consulta hace menos trabajo". Esto importa especialmente cuando las dimensiones no son pequeñas.

Considera una dimensión cliente con 5 millones de filas, normalizada como `dim_cliente → dim_ciudad → dim_estado` (jerarquía geográfica en copo de nieve) contra la misma dimensión desnormalizada donde `estado` vive directamente en `dim_cliente`.

Para responder "ventas totales por estado":

- **Estrella:** `fact_ventas` (100M filas) hace un solo join contra `dim_cliente` (5M filas), que ya trae `estado`.
- **Copo de nieve:** `fact_ventas` hace join contra `dim_cliente` (5M filas), y ese resultado intermedio vuelve a hacer join contra `dim_ciudad` (5K filas) y luego contra `dim_estado` (32 filas).

El segundo camino no es más caro por las tablas pequeñas (`dim_ciudad`, `dim_estado`) — es más caro porque agrega **etapas de shuffle** sobre un resultado intermedio ya grande (la salida del primer join, del tamaño de `fact_ventas`). Cada hop adicional en la cadena de joins vuelve a mover ese volumen de datos por la red del clúster.

Esto se traduce directamente en costo facturable:

- **BigQuery** cobra por bytes escaneados/procesados; más joins sobre resultados intermedios grandes significan más bytes movidos y más slots ocupados.
- **Snowflake** cobra por tiempo de cómputo del warehouse (créditos); una cadena de joins más larga generalmente implica más segundos de warehouse activo para la misma pregunta.

**Regla práctica:** la redundancia de un esquema estrella es barata cuando vive en una dimensión (se paga una vez, al cargar). El costo de un join extra se paga en **cada consulta**, multiplicado por cuántas veces se ejecuta esa consulta al día. Por eso los data marts orientados a BI casi siempre prefieren estrella, y el copo de nieve se reserva para jerarquías muy grandes, compartidas entre muchos data marts, donde mantener una sola versión de la verdad importa más que el costo por consulta.

## Esquema de Constelación (Fact Constellation / Galaxy Schema)

Cuando dos o más procesos de negocio comparten las mismas dimensiones, tiene sentido que compartan también las tablas físicas: eso es un **esquema de constelación**. En vez de una estrella aislada por proceso, varias tablas de hechos se conectan a un conjunto de **dimensiones conformadas** (misma clave, mismo grano, mismo significado en todos los hechos que la usan).

Retomando el dominio hospitalario: además de `FACT_CANCELACION_CITA` (una fila por cita programada), el hospital también factura por cada servicio prestado. En vez de crear dimensiones nuevas y redundantes para ese segundo proceso, se reutilizan `DIM_FECHA`, `DIM_HOSPITAL`, `DIM_SERVICIO`, `DIM_PACIENTE` y `DIM_ASEGURADORA`:

```{mermaid}
erDiagram

    FACT_CANCELACION_CITA {
        int appointment_key
        int fecha_key
        int hospital_key
        int servicio_key
        int paciente_key
        int aseguradora_key
        int cancelled_flag
        int no_show_flag
    }

    FACT_FACTURACION_HOSPITALARIA {
        int factura_key
        int fecha_key
        int hospital_key
        int servicio_key
        int paciente_key
        int aseguradora_key
        decimal monto_facturado
        decimal monto_cubierto
        decimal monto_paciente
    }

    DIM_FECHA {
        int fecha_key
        date fecha
        int mes
        int anio
    }

    DIM_HOSPITAL {
        int hospital_key
        string hospital_nombre
        string estado
    }

    DIM_SERVICIO {
        int servicio_key
        string servicio_nombre
        string especialidad
    }

    DIM_PACIENTE {
        int paciente_key
        int edad
        string genero
    }

    DIM_ASEGURADORA {
        int aseguradora_key
        string aseguradora_nombre
        string tipo
    }

    FACT_CANCELACION_CITA }o--|| DIM_FECHA : fecha
    FACT_CANCELACION_CITA }o--|| DIM_HOSPITAL : hospital
    FACT_CANCELACION_CITA }o--|| DIM_SERVICIO : servicio
    FACT_CANCELACION_CITA }o--|| DIM_PACIENTE : paciente
    FACT_CANCELACION_CITA }o--|| DIM_ASEGURADORA : aseguradora

    FACT_FACTURACION_HOSPITALARIA }o--|| DIM_FECHA : fecha
    FACT_FACTURACION_HOSPITALARIA }o--|| DIM_HOSPITAL : hospital
    FACT_FACTURACION_HOSPITALARIA }o--|| DIM_SERVICIO : servicio
    FACT_FACTURACION_HOSPITALARIA }o--|| DIM_PACIENTE : paciente
    FACT_FACTURACION_HOSPITALARIA }o--|| DIM_ASEGURADORA : aseguradora
```

Esto resuelve exactamente el tipo de redundancia más caro: sin dimensiones conformadas, cada equipo que construye un nuevo data mart terminaría creando su propia versión de "paciente" o "servicio", con distinta granularidad, distintos códigos y sin forma de cruzar ambos hechos ("¿cuántas citas canceladas generaron facturación parcial de cualquier forma?"). Con dimensiones conformadas, ambos hechos son comparables y combinables por las mismas llaves.

## Patrones de diseño para gestionar redundancia

Además de estrella, copo de nieve y constelación, hay patrones puntuales para decidir *dónde* vive cada dato — evitando tanto la redundancia descontrolada como los joins innecesarios.

```{=html}
<div id="patrones-redundancia" role="figure" aria-labelledby="pr-titulo" aria-describedby="pr-descripcion">
<style>
#patrones-redundancia {
  --texto:#172033;
  --secundario:#53627a;
  --linea:#9aa8bc;
  --fondo:#eef3f8;
  --azul:#dceafe;
  --azul-borde:#4478bd;
  --verde:#dff3e7;
  --verde-borde:#3f855c;
  --ambar:#fff0cc;
  --ambar-borde:#a56c10;
  --violeta:#eee5fb;
  --violeta-borde:#7655aa;
  --rojo:#fae4e2;
  --rojo-borde:#a4544c;
  --cian:#dcf2f3;
  --cian-borde:#327d82;

  box-sizing:border-box;
  max-width:1120px;
  margin:1.5rem auto;
  padding:.25rem;
  color:var(--texto);
  font-family:Inter,ui-sans-serif,system-ui,-apple-system,
    BlinkMacSystemFont,"Segoe UI",sans-serif;
}

#patrones-redundancia * {
  box-sizing:border-box;
}

#patrones-redundancia .titulo {
  margin:0 0 .35rem;
  text-align:center;
  font-size:clamp(1.15rem,2.4vw,1.65rem);
  font-weight:700;
}

#patrones-redundancia .subtitulo {
  max-width:760px;
  margin:0 auto 1.4rem;
  color:var(--secundario);
  text-align:center;
  line-height:1.45;
}

#patrones-redundancia .flujo {
  display:grid;
  grid-template-columns:minmax(180px,.8fr) 42px minmax(0,2.2fr);
  align-items:center;
  gap:.55rem;
}

#patrones-redundancia .pregunta {
  padding:1rem;
  border:2px solid var(--texto);
  border-radius:14px;
  background:var(--fondo);
  text-align:center;
  font-weight:700;
  line-height:1.35;
}

#patrones-redundancia .flecha {
  color:var(--secundario);
  font-size:1.65rem;
  font-weight:700;
  text-align:center;
}

#patrones-redundancia .rejilla {
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:.8rem;
}

#patrones-redundancia .patron {
  min-width:0;
  padding:.9rem 1rem;
  border:1px solid var(--linea);
  border-left:6px solid var(--acento);
  border-radius:10px;
  background:var(--superficie);
}

#patrones-redundancia .patron h3 {
  margin:0 0 .35rem;
  font-size:1rem;
  font-weight:700;
}

#patrones-redundancia .patron p {
  margin:0;
  color:var(--secundario);
  font-size:.9rem;
  line-height:1.4;
}

#patrones-redundancia code {
  color:var(--texto);
  font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
  font-size:.86em;
}

#patrones-redundancia .objetivo {
  display:inline-block;
  margin-top:.55rem;
  padding:.15rem .5rem;
  border:1px solid var(--acento);
  border-radius:999px;
  background:rgba(255,255,255,.5);
  color:var(--texto);
  font-size:.76rem;
  font-weight:700;
}

#patrones-redundancia .p1 {
  --superficie:var(--azul);
  --acento:var(--azul-borde);
}

#patrones-redundancia .p2 {
  --superficie:var(--verde);
  --acento:var(--verde-borde);
}

#patrones-redundancia .p3 {
  --superficie:var(--ambar);
  --acento:var(--ambar-borde);
}

#patrones-redundancia .p4 {
  --superficie:var(--violeta);
  --acento:var(--violeta-borde);
}

#patrones-redundancia .p5 {
  --superficie:var(--rojo);
  --acento:var(--rojo-borde);
}

#patrones-redundancia .p6 {
  --superficie:var(--cian);
  --acento:var(--cian-borde);
}

#patrones-redundancia .balance {
  display:grid;
  grid-template-columns:1fr auto 1fr;
  align-items:center;
  gap:.8rem;
  margin-top:1.25rem;
  padding-top:1rem;
  border-top:1px solid var(--linea);
  color:var(--secundario);
  text-align:center;
  font-size:.88rem;
  font-weight:700;
}

#patrones-redundancia .escala {
  width:min(210px,28vw);
  height:8px;
  border-radius:999px;
  background:linear-gradient(
    90deg,
    var(--azul-borde),
    var(--verde-borde)
  );
}

#patrones-redundancia .solo-lector {
  position:absolute;
  width:1px;
  height:1px;
  margin:-1px;
  padding:0;
  overflow:hidden;
  clip:rect(0,0,0,0);
  white-space:nowrap;
  border:0;
}

@media (max-width:760px) {
  #patrones-redundancia .flujo {
    grid-template-columns:1fr;
  }

  #patrones-redundancia .flecha {
    transform:rotate(90deg);
  }
}

@media (max-width:520px) {
  #patrones-redundancia .rejilla {
    grid-template-columns:1fr;
  }

  #patrones-redundancia .balance {
    grid-template-columns:1fr;
  }

  #patrones-redundancia .escala {
    width:75%;
    margin:0 auto;
  }
}

/* @media (prefers-color-scheme:dark) {
  #patrones-redundancia {
    --texto:#eef4ff;
    --secundario:#bdc8d8;
    --linea:#64748b;
    --fondo:#243044;
    --azul:#203a5d;
    --azul-borde:#83b5f4;
    --verde:#203f31;
    --verde-borde:#79c696;
    --ambar:#4b391a;
    --ambar-borde:#e2b75d;
    --violeta:#392d50;
    --violeta-borde:#b79ae0;
    --rojo:#4b2c2c;
    --rojo-borde:#df9189;
    --cian:#213f45;
    --cian-borde:#77c1c5;
  }

  #patrones-redundancia .objetivo {
    background:rgba(0,0,0,.18);
  }
} */
</style>

<h2 id="pr-titulo" class="titulo">
  Patrones para decidir dónde vive cada dato
</h2>

<p id="pr-descripcion" class="subtitulo">
  La elección depende de qué se comparte, qué identifica al hecho,
  qué cambia y qué cardinalidad existe.
</p>

<div class="flujo">
  <div class="pregunta">
    ¿Qué problema de modelado se necesita resolver?
  </div>

  <div class="flecha" aria-hidden="true">&#8594;</div>

  <div class="rejilla">
    <section class="patron p1">
      <h3>Dimensión conformada</h3>
      <p>
        Una dimensión común sirve a varios hechos con el mismo
        grano, llaves y reglas.
      </p>
      <span class="objetivo">Reutilizar sin duplicar</span>
    </section>

    <section class="patron p2">
      <h3>Dimensión degenerada</h3>
      <p>
        El identificador permanece en el hecho porque no tiene
        atributos propios.
      </p>
      <span class="objetivo">Evitar una tabla vacía</span>
    </section>

    <section class="patron p3">
      <h3>Junk dimension</h3>
      <p>
        Los indicadores de baja cardinalidad se agrupan como
        combinaciones en una dimensión.
      </p>
      <span class="objetivo">Agrupar columnas dispersas</span>
    </section>

    <section class="patron p4">
      <h3>Role-playing dimension</h3>
      <p>
        Una dimensión física asume varios roles; por ejemplo,
        pedido, envío y pago sobre <code>DIM_FECHA</code>.
      </p>
      <span class="objetivo">Reutilizar mediante alias</span>
    </section>

    <section class="patron p5">
      <h3>Mini-dimensión</h3>
      <p>
        Los atributos volátiles se separan para no versionar
        toda una dimensión grande.
      </p>
      <span class="objetivo">Aislar cambios frecuentes</span>
    </section>

    <section class="patron p6">
      <h3>Tabla puente</h3>
      <p>
        Una relación muchos a muchos se representa sin duplicar
        filas de la tabla de hechos.
      </p>
      <span class="objetivo">Preservar la cardinalidad</span>
    </section>
  </div>
</div>

<div class="balance" aria-label="Equilibrio buscado">
  <span>Menos redundancia descontrolada</span>
  <span class="escala" aria-hidden="true"></span>
  <span>Menos joins innecesarios</span>
</div>

<p class="solo-lector">
  El diagrama presenta seis patrones de diseño que equilibran
  la redundancia y la complejidad de consulta.
</p>
</div>
```

**Dimensiones conformadas.** Una misma dimensión (mismo grano, mismas llaves) reutilizada por varios hechos, como `DIM_FECHA` en el ejemplo anterior. Es la base de cualquier esquema de constelación y evita que cada data mart reinvente "cliente" o "producto" con reglas distintas.

**Dimensiones degeneradas.** Un atributo que identifica al hecho (folio de orden, número de ticket) pero que no tiene atributos propios que ameriten una tabla de dimensión. Se guarda como columna directa en la tabla de hechos en vez de crear una dimensión de una sola columna.

**Junk dimensions.** Varios indicadores booleanos o de baja cardinalidad (`es_promocion`, `metodo_pago`, `canal`) se combinan en una sola dimensión "basurero" con una fila por combinación posible, en vez de agregar media docena de columnas sueltas al hecho o crear media docena de dimensiones diminutas.

**Role-playing dimensions.** La misma tabla física de dimensión se usa varias veces con distinto significado — por ejemplo, `DIM_FECHA` referenciada como fecha de pedido, fecha de envío y fecha de pago dentro del mismo hecho, mediante alias en la consulta (`fecha_pedido_key`, `fecha_envio_key`, `fecha_pago_key`, todas apuntando a la misma `DIM_FECHA`).

**Mini-dimensiones.** Cuando algunos atributos de una dimensión grande cambian con mucha frecuencia (ingresos estimados de un cliente, score de riesgo) separarlos en una dimensión pequeña propia evita que esos cambios disparen versiones nuevas de SCD tipo 2 en toda la fila del cliente — tema que se retoma en el siguiente capítulo.

**Bridge tables.** Cuando la relación entre el hecho y una dimensión es de muchos a muchos (un estudiante con varias becas activas, un diagnóstico con múltiples médicos tratantes) una tabla puente resuelve la relación sin duplicar filas del hecho ni forzar una cardinalidad que no existe en el negocio.

## Conclusión

Estrella, copo de nieve y constelación no son alternativas puramente estéticas: cada una mueve el costo de un lugar a otro (almacenamiento vs. cómputo por consulta vs. complejidad de mantenimiento). En un motor columnar moderno, la redundancia dentro de una dimensión suele ser la opción más barata. Lo que todavía no se ha cubierto es qué pasa cuando esas dimensiones **cambian** — un cliente se muda de ciudad, un producto cambia de categoría. Ese es el tema del siguiente capítulo: dimensiones lentamente cambiantes (SCD).
